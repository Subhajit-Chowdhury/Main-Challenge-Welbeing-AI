# Application Core (/app)

This directory is reserved for application-wide modules, global states, and high-level routing setups.

## Structure
- App-wide routing configurations
- Root level state adapters (e.g. Redux, Zustand, Context, or pure React controllers)
- Global PWA service worker event observers and installation triggers
